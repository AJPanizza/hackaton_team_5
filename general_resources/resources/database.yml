resources:
  database_instances:
    hackathon_db:
      name: "${var.team_prefix}-cgm-db"
      capacity: CU_1

  database_catalogs:
    hackathon_catalog:
      name: "${var.team_prefix}_lakebase_catalog"
      database_instance_name: ${resources.database_instances.hackathon_db.name}
      database_name: "${var.team_prefix}_db"
      create_database_if_not_exists: true